using System;
using System.Collections.Generic;
using System.Text;

namespace FLAMG.Other.LintCode100
{
    public class MaximumSubarrayDifference
    {
        //public int MaxDiffSubArray(int[] nums)
        //{
        //    if (nums == null || nums.Length == 0)
        //        return 0;
        //    int posMaxSum = 0, posMinSum = 0;
        //    int negMaxSum = 0, negMinSum = 0;
        //    int i = 0;
        //    while(i < nums.Length)
        //    {
        //        int posSum = 0, negSum = 0;
        //        while(nums[i] >= 0)
        //        {
        //            posSum += nums[i];
        //            i++;
        //        }
        //        posMaxSum = Math.Max(posMaxSum, posSum);
        //        while(nums[i] < 0)
        //        {
        //            negSum += nums[i];
        //            i++;
        //        }
        //        negMinSum = Math.Min(negMinSum, negSum);
        //    }
        //}
    }
}
